# Roadmap

当前明确不做：

- 自动创建 PR、自动合并 main、CI/CD 自动部署
- 企业 SSO / OAuth
- 外部向量数据库与向量记忆检索
- 第二套代码 Planner 或与 Supervisor 并存的编排架构

后续只在真实使用数据证明必要时增加候选工件持久化、更细粒度工具策略或更复杂的仓库调度；扩展继续复用 `ExecutionProvider` 协议，不复制生命周期。
