server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  location /api/ {
    proxy_pass http://${V5_CONTROL_PLANE_HOST}:8085;
  }

  location /logout {
    proxy_pass http://${V5_CONTROL_PLANE_HOST}:8085;
  }

  location / {
    try_files $uri /index.html;
  }
}
